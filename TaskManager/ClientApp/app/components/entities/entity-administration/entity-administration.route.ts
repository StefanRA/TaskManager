import { Routes } from '@angular/router';

import { EntityAdministrationComponent } from './entity-administration.component';

export const entityAdministrationRoute: Routes = [
    {
        path: 'entity-administration',
        component: EntityAdministrationComponent
    }
];